# blog

To install dependencies:

```bash
bun install
```

To build the site:

```bash
bun run build
```

The generated HTML is written to `output/`.

This project was created using `bun init` in bun v1.3.14. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
